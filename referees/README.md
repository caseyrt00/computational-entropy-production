Four blind referee passes, each on a disjoint part of the paper (A: the core theorems; B: the
restatements of known results; C: the claims and their labels; D: the later additions), run by
an AI system before submission, with every referee required to re-derive a statement before
reading its proof. The paper's fixes in response to each report are logged inside that report.
